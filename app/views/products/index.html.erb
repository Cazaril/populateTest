<h1>Product</h1>

  <div><%= @product.title %></div>
  <div><%= @product.rating.round(1) %></div>
  <h2>Ratings</h2>
<ul>
  <% @product.reviews.each do |review| %>
    <li>
      <%= review.review %>. Rating:  <%= review.rating %>
    </li>
  <% end %>
</ul>